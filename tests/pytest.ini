[pytest]
addopts = --strict-markers
          --cov-config=.coveragerc
          --cov-report term-missing
          --cov=hashed_file
          --cov=duplicate_finder
          -vvv
markers =
    raises: Test for raising an exception
